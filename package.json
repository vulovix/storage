{
    "name": "komplexica-files",
    "scripts": {
        "dev": "nodemon server.js",
        "start": "node server.js",
        "build": "cp server.js dist && cp package.json dist && cp -r public dist && cp data.txt dist"
    },
    "dependencies": {
        "express": "^4.18.1",
        "cloudinary": "^1.30.0"
    },
    "devDependencies": {
        "fs-extra": "^10.1.0",
        "dotenv": "^16.0.1",
        "nodemon": "^2.0.16",
        "pkg":"^5.8.1"
    }
}